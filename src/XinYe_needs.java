import java.util.*;
public class XinYe_needs {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        while (n-- > 0) {
            //前端工时
            int F = sc.nextInt();
            //后端工时
            int B = sc.nextInt();
            //联调工时
            int L = sc.nextInt();
            //测试工时
            int T= sc.nextInt();
            //必须前后端完成后才可联调；必须联调后才可测试；

            //输出：总完成时间最短天数
        }
    }
}
